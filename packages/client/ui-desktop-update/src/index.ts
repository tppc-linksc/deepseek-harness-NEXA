/**
 * Desktop update surface plugin, host half. Electron owns release discovery,
 * installer storage, verification, and launch through private protocol routes.
 */

/** Host plugin body — no Host-process behavior for this desktop-only surface. */
export function apply(): void {}
