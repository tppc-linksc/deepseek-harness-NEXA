# Mount the desktop-only browser plugin over the shipped Web composition. The
# browser test intercepts its Electron-owned private routes at the transport
# edge; all client loading, slot composition, localization, and CSS remain the
# real built product path.
- insert:
    - id: ui-desktop-update-test
      name: '@deepseek-ai/dsh-client-ui-desktop-update'
